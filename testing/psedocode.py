#import pygame and sys
#define window height and width and name 
#screen = pygame.display.set_mode((WIDTH, HEIGHT))

#font  = pygame.font (font,size)
#set biggest character width and height(capital A)

#grid size 
#cols = width// charcter width
#rows = height // charcter height

#bg_colors = (color code )
#color_text = (color code)

#class cell or area you text (has to be init since its base)
#def __init__ (self,char = "",color = color_text, image = none)
    #selt.char = char 
    #self.color